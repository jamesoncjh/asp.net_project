@model IEnumerable<ThAmCo.Events.Data.GuestBooking>

@{
    ViewData["Title"] = "Guest Booking";
}

<h2>Guest Booking List</h2>

<p>
    <a asp-area="" asp-controller="Home" asp-action="Index">Back to Homepage</a> |
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Customer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Event)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Attended)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Customer.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Event.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Attended)
            </td>
            <td>
            @*    @Html.ActionLink("Edit", "Edit", new {  /*id=item.PrimaryKey*/  }) |
                @Html.ActionLink("Details", "Details", new {  /*id=item.PrimaryKey*/  }) |
                @Html.ActionLink("Delete", "Delete", new {  /*id=item.PrimaryKey  */})*@
                <a asp-action="Edit" asp-route-customerId="@item.CustomerId" asp-route-eventId="@item.EventId">Edit</a> |
                <a asp-action="Details" asp-route-customerId="@item.CustomerId" asp-route-eventId="@item.EventId">Details</a> |
                <a asp-action="Delete" asp-route-customerId="@item.CustomerId" asp-route-eventId="@item.EventId">Delete</a>
            </td>
        </tr>
        
}
    </tbody>
</table>
